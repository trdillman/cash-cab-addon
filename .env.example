# CashCab (Blender addon) local environment overrides
#
# Copy this file to `.env` and fill in values.
# NOTE: `.env` is ignored by git (see `.gitignore`).

# Google Maps API key used for auto-snap in headless/bulk workflows.
# The addon reads this via `CASHCAB_GOOGLE_API_KEY`.
CASHCAB_GOOGLE_API_KEY=

# Optional: if you already store it under a different name, you can keep it
# here too for reference (not read by the addon unless you export it yourself).
MAPS_GOOGLE_API_KEY=

